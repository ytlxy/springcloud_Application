server:
  port: 80

spring:
  application:
    name: cloud-order-service

eureka:
  client:
    #是否注册进Eureka
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka